{
  "name": "@sourdough/shared",
  "version": "1.0.0",
  "description": "Shared code between web and mobile Sourdough app",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf *.js *.d.ts types/*.js types/*.d.ts utils/*.js utils/*.d.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet for shared package\" && exit 0"
  },
  "keywords": [
    "sourdough",
    "shared",
    "monorepo"
  ],
  "author": "cmollenbach",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^19.2.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "react": "^19.2.0",
    "zustand": "^5.0.8"
  }
}
